const N = 4
const D1 = 3
const D2 = 2

input x: [N, D1]

param W: [D1, D2]

forward {
  let a = matmul(x, W)
  let b = relu(a)
  let c = concat(1, a, b)
  let d = slice_rows(c, 0, 2)
  let e = dropout(d, 0.1)
  return e
}

